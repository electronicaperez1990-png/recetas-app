[project]
name = "recetas-app"
version = "0.1.0"
description = "Aplicación web de recetas de cocina (arquitectura MVC, Flask)"
authors = [
    { name = "electronicaperez1990-png", email = "electronicaperez1990@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
